# calculates the multiple of minimum wage the user earns
import math
minimunWage = float(input("Minimum wage: "))
wage = float(input("Wage: "))
r = int(wage / minimunWage)
print("You have %d times the minimum wage" %(r))